# Base URL of the kidora-be backend API (shared with the customer-facing
# Kidora-fe app — this is the same server, just a different frontend).
# Copy this file to .env (or .env.local) and adjust for your environment.
VITE_API_BASE_URL=http://localhost:3000
